version: '3.8'

services:
  telegram-bot:
    image: yourdockerhubusername/faq_telegrambot_pelican:latest
    container_name: faq-telegram-bot
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
    ports:
      - "8443:8443"
    volumes:
      - ./data:/app/data
