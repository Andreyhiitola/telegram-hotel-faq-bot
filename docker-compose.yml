version: '3.8'

services:
  telegram-bot:
    build: .
    container_name: faq-telegram-bot
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
    volumes:
      - ./bot.py:/app/bot.py
