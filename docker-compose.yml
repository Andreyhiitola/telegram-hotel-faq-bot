version: '3.8'

services:
  telegram-bot:
    build: .
    container_name: faq_telegrambot_pelican:latest
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
    volumes:
      - ./bot.py:/app/bot.py
