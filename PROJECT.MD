# 📚 Полная документация Telegram FAQ Bot

**Комбинированная документация:** Пользовательская + Техническая + Архитектура + Развёртывание

---

## 📖 Содержание

1. [Обзор и возможности](#-1-обзор-и-возможности)
2. [Быстрый старт](#-2-быстрый-старт)
3. [Использование](#-3-использование)
4. [Настройка под гостиницу](#-4-настройка-под-гостиницу)
5. [Управление ботом](#-5-управление-ботом)
6. [Техническая архитектура](#-6-техническая-архитектура)
7. [Структура данных](#-7-структура-данных)
8. [CI/CD и автоматизация](#-8-cicd-и-автоматизация)
9. [Развёртывание](#-9-развёртывание)
10. [Мониторинг и отладка](#-10-мониторинг-и-отладка)
11. [Безопасность](#-11-безопасность)
12. [Масштабируемость](#-12-масштабируемость)
13. [Troubleshooting](#-13-troubleshooting)
14. [Полезные ссылки](#-14-полезные-ссылки)

---

## 🎯 1. Обзор и возможности

### Что такое Telegram FAQ Bot?

Автоматизированный Telegram бот для гостиниц, который предоставляет мгновенные ответы на часто задаваемые вопросы гостей 24/7.

### Для кого?

- 🏨 Малые и средние гостиницы
- 🏠 Хостелы и гостевые дома
- 🏢 Апарт-отели
- 🛏️ Мини-отели

### Ключевые преимущества

- ⚡ **Быстрый запуск** — готов за 5 минут
- 🎨 **14 готовых разделов FAQ** — покрывают все основные вопросы
- 🐳 **Docker** — простое развёртывание без зависимостей
- 🔧 **Легкая настройка** — меняйте тексты без программирования
- 🌐 **Работает везде** — не требует внешнего IP или SSL
- 🔄 **Надежность** — автоматический перезапуск при сбоях
- 🚀 **CI/CD готов** — автоматический деплой на VPS

### Содержание бота (14 разделов)

| **Основная информация** | **Услуги** | **Дополнительно** | **Правила** |
|---|---|---|---|
| 🏨 Заезд и выезд | 🍳 Завтрак и питание | 👶 Дети и животные | 🚨 Правила и безопасность |
| 💳 Оплата и бронирование | 🚗 Парковка | 🎭 Экскурсии | 🎁 Акции и спецпредложения |
| 🛏️ Что входит в номер | 🏊 Бассейн, фитнес, SPA | 🏥 Медицинские услуги | |
| 📞 Контакты и адрес | 📶 Wi-Fi и связь | 💼 Бизнес-услуги | |

---

## 🚀 2. Быстрый старт

### Предварительные требования

- ✅ Docker и Docker Compose установлены
- ✅ Токен бота от Telegram @BotFather
- ✅ Доступ в интернет

### Установка за 3 шага

**Шаг 1: Клонируйте репозиторий**

```bash
git clone https://github.com/Andreyhiitola/telegram-hotel-faq-bot.git
cd telegram-hotel-faq-bot
```

**Шаг 2: Получите токен бота**

1. Откройте Telegram и найдите @BotFather
2. Отправьте команду `/newbot`
3. Следуйте инструкциям бота
4. Скопируйте полученный токен

**Шаг 3: Настройте и запустите**

```bash
# Создайте файл с токеном
cp .env.example .env

# Отредактируйте .env и вставьте ваш токен
nano .env

# Запустите бота
docker-compose up -d --build

# Проверьте логи
docker-compose logs -f
```

**🎉 Готово!** Бот запущен и работает!

---

## 🎮 3. Использование

### Для гостей

1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Выберите интересующий раздел из меню
4. Получите мгновенный ответ

### Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Показать главное меню |
| `/help` | Справка по использованию |
| Клик на кнопку | Отправить выбранную категорию |

### Пример взаимодействия

**Пользователь:** `/start`

**Бот:**
```
Добро пожаловать! 👋

Выберите интересующий вас раздел:
[🏨] [💳] [🛏️] [📞]
[🍳] [🚗] [🏊] [📶]
[👶] [🎭] [🏥] [💼]
[🚨] [🎁]
```

**Пользователь:** Нажимает `🏨 Заезд и выезд`

**Бот:**
```
🏨 Заезд и выезд

Время заезда: после 14:00
Время выезда: до 11:00

Нужна ранняя регистрация? 
Обратитесь в front-desk!

📞 +7-XXX-XXX-XX-XX
```

---

## ⚙️ 4. Настройка под гостиницу

### Редактирование FAQ

Откройте файл `bot.py` и найдите словарь `FAQ` (примерно строка 6):

```python
FAQ = {
    "🏨 Заезд и выезд": {
        "text": "🏨 <b>Заголовок</b>\n\nВаш текст с HTML-форматированием",
        "emoji": "🏨"
    },
    "💳 Оплата и бронирование": {
        "text": "💳 <b>Как оплатить?</b>\n\n...",
        "emoji": "💳"
    },
    # ... добавьте свои разделы
}
```

### Поддерживаемое форматирование HTML

```
<b>жирный текст</b>
<i>курсив</i>
<u>подчёркнутый</u>
<s>зачёркнутый</s>
<code>код</code>
<pre>блок кода</pre>
\n - перенос строки
\n\n - пустая строка между абзацами
```

### Изменение контактов

Найдите раздел `"📞 Контакты и адрес"` и измените:

- Телефон
- Email
- Адрес
- Способы проезда
- Информацию о трансфере

### Добавление нового раздела

```python
FAQ = {
    # ... существующие разделы ...
    
    "🎪 Новый раздел": {
        "text": "🎪 <b>Заголовок нового раздела</b>\n\nОписание...",
        "emoji": "🎪"
    }
}
```

### Применение изменений

```bash
# После изменения bot.py просто перезапустите
docker-compose restart

# Или пересоберите
docker-compose up -d --build
```

---

## 🛠 5. Управление ботом

### Основные команды

```bash
# Запустить бота
docker-compose up -d

# Остановить бота
docker-compose down

# Перезапустить бота
docker-compose restart

# Посмотреть логи в реальном времени
docker-compose logs -f

# Посмотреть последние 50 строк логов
docker-compose logs --tail=50

# Посмотреть статус контейнеров
docker-compose ps

# Пересобрать образ после изменения кода
docker-compose up -d --build

# Просмотр использования ресурсов
docker stats faq-telegram-bot
```

### Проверка работы

```bash
# Смотрим логи
docker-compose logs -f

# Должны увидеть примерно это:
# 🤖 Бот запущен и готов к работе...
# 📝 Загружено 14 FAQ разделов
```

---

## 🏗️ 6. Техническая архитектура

### Технологический стек

| Компонент | Технология | Версия |
|-----------|-----------|--------|
| **Runtime** | Python | 3.11+ |
| **Telegram API** | pyTelegramBotAPI | 4.14+ |
| **Контейнеризация** | Docker | 20.10+ |
| **Оркестрация** | Docker Compose | 2.0+ |
| **Развёртывание** | GitHub Actions | Latest |
| **Хранилище образов** | Docker Hub | Latest |
| **VCS** | Git | 2.30+ |

### Архитектурная диаграмма

```
┌─────────────┐
│   Telegram  │
│   User      │
└──────┬──────┘
       │ (Message)
       ↓
┌──────────────────┐
│ Telegram Bot API │
└────────┬─────────┘
         │ (Polling каждые 30 сек)
         ↓
┌──────────────────────────────┐
│   Docker Container           │
│ ┌──────────────────────────┐ │
│ │  Python 3.11             │ │
│ │  ┌────────────────────┐  │ │
│ │  │  bot.py            │  │ │
│ │  │  - FAQ Dictionary  │  │ │
│ │  │  - Message Handler │  │ │
│ │  │  - User Commands   │  │ │
│ │  └────────────────────┘  │ │
│ └──────────────────────────┘ │
└──────┬──────────────────────────┘
       │ (Send Message)
       ↓
┌─────────────────┐
│   Telegram      │
│   Chat          │
└─────────────────┘
```

### Поток данных

1. Пользователь отправляет сообщение в Telegram
2. Bot API получает обновление (polling каждые 30 сек)
3. `bot.py` обрабатывает сообщение
4. Бот отправляет ответ из словаря `FAQ`
5. Пользователь получает ответ

### Преимущества текущей архитектуры

✅ Нет необходимости в публичном IP
✅ Работает на любом сервере с интернетом
✅ Простая настройка
✅ Низкие требования к ресурсам
✅ Надежность и автоматический перезапуск

---

## 📊 7. Структура данных

### Структура FAQ в памяти

```python
FAQ = {
    "🏨 Заезд и выезд": {
        "text": "🏨 <b>Время заезда и выезда</b>\n\n...",
        "emoji": "🏨"
    },
    # ... ещё 13 разделов
}
```

### Формат хранения

- **Тип**: Python dictionary (в оперативной памяти)
- **Формат текста**: HTML-теги (совместимо с Telegram Bot API)
- **Кодировка**: UTF-8
- **Размер**: ~50 KB (в памяти)
- **Время загрузки**: < 1 сек при старте

### Жизненный цикл данных

1. При старте контейнера `bot.py` загружается в память
2. Dictionary `FAQ` инициализируется
3. Бот готов к обработке запросов
4. При изменении `bot.py` нужен перезапуск контейнера

---

## 🔄 8. CI/CD и автоматизация

### GitHub Actions Workflow

Два связанных workflow автоматизируют весь процесс:

#### Workflow 1: Build and Push Docker Image

```yaml
name: Build and Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  build_and_push:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Build Docker image
      run: docker build -t andreysagurov/faq_telegram_pelican:latest .

    - name: Push Docker image
      run: docker push andreysagurov/faq_telegram_pelican:latest
```

**Что происходит:**
1. При пуше в main ветку
2. GitHub Actions собирает Docker образ
3. Логинится в Docker Hub
4. Пушит образ в Docker Hub

#### Workflow 2: Deploy on VPS

```yaml
name: Deploy on VPS

on:
  workflow_run:
    workflows: ["Build and Push Docker Image"]
    types:
      - completed

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    steps:
    - name: SSH and deploy
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.VPS_HOST }}
        username: ${{ secrets.VPS_USER }}
        key: ${{ secrets.VPS_SSH_KEY }}
        script: |
          cd /root/telegram-hotel-faq-bot
          git pull origin main
          docker-compose down
          docker-compose pull
          docker-compose up -d
```

**Что происходит:**
1. После успешного завершения первого workflow
2. Подключается к VPS по SSH
3. Обновляет код из Git
4. Останавливает старые контейнеры
5. Загружает новые образы с Docker Hub
6. Запускает контейнеры с новыми образами

### CI/CD Pipeline (полный процесс)

```
Developer пушит в main
    ↓
GitHub Actions срабатывает
    ├─ Checkout код
    ├─ Build Docker image
    ├─ Push in Docker Hub
    └─ Trigger Deploy workflow
    ↓
Deploy workflow срабатывает (при success)
    ├─ SSH в VPS
    ├─ git pull origin main
    ├─ docker-compose down
    ├─ docker-compose pull
    └─ docker-compose up -d
    ↓
✅ Бот обновлен и перезапущен на VPS
```

### Необходимые GitHub Secrets

| Secret | Значение | Пример |
|--------|----------|--------|
| `DOCKERHUB_USERNAME` | Ваш username на Docker Hub | `andreysagurov` |
| `DOCKERHUB_TOKEN` | Personal Access Token | `dckr_pat_...` |
| `VPS_HOST` | IP или домен VPS | `192.168.1.100` |
| `VPS_USER` | Пользователь на VPS | `root` |
| `VPS_SSH_KEY` | Приватный SSH-ключ | `-----BEGIN RSA...` |

---

## 🚀 9. Развёртывание

### Локальное развёртывание (для разработки)

```bash
# 1. Клонирование
git clone https://github.com/Andreyhiitola/telegram-hotel-faq-bot.git
cd telegram-hotel-faq-bot

# 2. Настройка
cp .env.example .env
nano .env # вставить токен бота

# 3. Запуск
docker-compose up -d --build

# 4. Проверка
docker-compose logs -f
docker-compose ps
```

### Развёртывание на VPS (Production)

**Требования к VPS:**
- Ubuntu 20.04+ или Debian 11+
- Docker 20.10+
- Docker Compose 2.0+
- Минимум 1 GB RAM
- Минимум 10 GB SSD
- Публичный IP адрес
- SSH доступ

**Шаг 1: Подготовка VPS**

```bash
# На VPS установить Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker $USER

# Установить Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose
```

**Шаг 2: Клонирование проекта**

```bash
git clone https://github.com/Andreyhiitola/telegram-hotel-faq-bot.git
cd telegram-hotel-faq-bot

# Создайте .env файл
cp .env.example .env
nano .env # вставьте токен бота
```

**Шаг 3: Запуск бота**

```bash
docker-compose up -d --build

# Проверьте статус
docker-compose ps
docker-compose logs -f
```

**Шаг 4: Проверка автозапуска**

```bash
# Проверьте, что перезапуск включен
docker-compose ps

# При перезагрузке VPS бот запустится автоматически
# (благодаря restart: unless-stopped в docker-compose.yml)
```

### Автоматический деплой через GitHub Actions

После настройки GitHub Secrets, каждый пуш в main автоматически:

1. Собирает Docker образ
2. Пушит в Docker Hub
3. Деплоит на VPS
4. Перезапускает бота

Вам не нужно ничего делать!

---

## 🔍 10. Мониторинг и отладка

### Просмотр логов

```bash
# Все логи
docker-compose logs

# Последние 100 строк
docker-compose logs --tail=100

# В реальном времени
docker-compose logs -f

# Только ошибки
docker-compose logs | grep ERROR

# С временными метками
docker-compose logs --timestamps
```

### Проверка использования ресурсов

```bash
# CPU, Memory, Network
docker stats faq-telegram-bot

# Полная информация контейнера
docker inspect faq-telegram-bot

# Размер образа
docker images | grep faq_telegram_pelican
```

### Интерактивная отладка

```bash
# Зайти в контейнер
docker-compose exec faq-telegram-bot bash

# Внутри контейнера проверить Python
python --version

# Проверить наличие зависимостей
python -c "import telebot; print(telebot.__version__)"

# Проверить FAQ словарь
python -c "from bot import FAQ; print(len(FAQ), 'разделов')"

# Выход
exit
```

### Метрики и KPI

| Метрика | Как проверить | Целевое значение |
|---------|---------------|-----------------|
| CPU usage | `docker stats` | < 50% |
| Memory | `docker stats` | < 100 MB |
| Uptime | `docker-compose ps` | > 99% |
| Response time | Логи | < 1 сек |
| Active users | Логи | Зависит |

---

## 🔐 11. Безопасность

### Защита токена бота

**Проблема:** Telegram Bot токен — это ключ доступа к боту

**Решения:**

1. **Локальная разработка**
   ```
   .env - не загружается в git
   .gitignore - исключает .env
   git-auto.sh - локальный скрипт
   ```

2. **Production (GitHub Actions)**
   ```
   GitHub Secrets - шифрованное хранилище
   Доступны только в workflow
   Не логируются в вывод
   ```

3. **VPS**
   ```
   .env файл в /root/telegram-hotel-faq-bot/
   Ограничены права доступа (600)
   Не в репозитории
   ```

### Лучшие практики безопасности

✅ Никогда не коммитьте `.env` файл
✅ Используйте GitHub Secrets для CI/CD
✅ Создавайте токен только в @BotFather
✅ Регулярно обновляйте зависимости
✅ Используйте HTTPS для всех соединений
✅ Ограничивайте доступ к VPS по SSH ключам
✅ Используйте брандмауэр на VPS

### Защита от атак

| Атака | Защита |
|-------|--------|
| Перехват токена | HTTPS, GitHub Secrets |
| Утечка в логах | Маскирование чувствительных данных |
| Перебор токена | Rate limit Telegram API |
| Несанкционированный доступ | Приватный репозиторий, SSH ключи |
| DDoS | Telegram API rate limiting |

---

## 📈 12. Масштабируемость

### Текущие ограничения

| Параметр | Ограничение | Заметки |
|----------|------------|--------|
| FAQ разделов | 14 | Мягкое ограничение, можно добавить больше |
| Размер ответа | ~2000 символов | Ограничение Telegram |
| Одновременных пользователей | 1000+ | Зависит от VPS |
| Requests в сек | 100+ | Ограничение Telegram API |
| Память | ~100 MB | Хранение FAQ в памяти |

### Текущая архитектура подходит для:

- ✅ 1000+ пользователей в день
- ✅ 100+ активных пользователей одновременно
- ✅ Малые и средние гостиницы
- ✅ Пиковые нагрузки

### План масштабирования (будущее)

#### Уровень 1: Webhook (расширенный)
- Смена с polling на webhook
- Нужен публичный IP/домен
- Подходит для 10000+ пользователей в день

#### Уровень 2: Микросервисы
- Отделение logic от API
- Кэширование ответов (Redis)
- Очередь задач (RabbitMQ)
- Подходит для 100000+ пользователей в день

#### Уровень 3: Высокая доступность
- Несколько инстансов бота
- Load balancer
- Распределенное хранилище FAQ
- 99.99% uptime SLA

---

## 🐛 13. Troubleshooting

### Бот не отвечает

**Проверьте статус контейнера:**

```bash
docker-compose ps

# Должно быть: Up (running)
```

**Посмотрите логи:**

```bash
docker-compose logs --tail=50
```

**Перезапустите бота:**

```bash
docker-compose restart
```

### Ошибка "Bad Request: unauthorized"

**Проблема:** Неправильный токен

**Решение:**
1. Проверьте токен в `.env`
2. Убедитесь, что нет пробелов
3. Получите новый токен в @BotFather
4. Перезапустите бот

### Контейнер постоянно перезапускается

**Проверьте логи:**

```bash
docker-compose logs --tail=100
```

**Возможные причины:**
- Неправильный токен
- Синтаксическая ошибка в `bot.py`
- Отсутствие зависимостей

**Решение:**
```bash
# Пересоберите образ
docker-compose down
docker-compose up -d --build
```

### Ошибка при пуше в GitHub

**"push declined due to repository rule violations"**

Решение:
1. Перейдите в Settings → Branches
2. Отредактируйте Branch protection rules
3. Отключите строгие требования или используйте Pull Request

### Docker image не обновляется

**Проблема:** Старый образ используется

**Решение:**

```bash
# Удалить старый образ
docker rmi andreysagurov/faq_telegram_pelican:latest

# Пересоздать
docker-compose up -d --build

# Или pull новый
docker-compose pull
docker-compose up -d
```

### Недостаточно места на VPS

**Очистить Docker:**

```bash
# Удалить неиспользуемые образы
docker image prune -a

# Удалить неиспользуемые контейнеры
docker container prune

# Удалить все (осторожно!)
docker system prune -a
```

### SSH соединение не работает на VPS

**Проверьте доступ:**

```bash
ssh -i /path/to/key root@VPS_IP

# Если не работает, проверьте:
# 1. IP адрес верный
# 2. SSH ключ имеет права 600
# 3. Брандмауэр открыт на порт 22
```

---

## 🔗 14. Полезные ссылки

### Документация

- [Telegram Bot API](https://core.telegram.org/bots/api) — полная документация API
- [pyTelegramBotAPI](https://github.com/eternnoir/pyTelegramBotAPI) — библиотека
- [Docker Documentation](https://docs.docker.com/) — Docker и контейнеры
- [GitHub Actions](https://docs.github.com/en/actions) — CI/CD
- [Docker Compose](https://docs.docker.com/compose/) — оркестрация контейнеров

### Инструменты

- [BotFather](https://t.me/botfather) — создание бота в Telegram
- [Docker Hub](https://hub.docker.com/) — реестр Docker образов
- [GitHub](https://github.com/) — хостинг кода

### Примеры проектов

- [python-telegram-bot samples](https://github.com/python-telegram-bot/python-telegram-bot/tree/master/examples)
- [Awesome Telegram Bots](https://github.com/ebertti/awesome-telegram)

### Рекомендуемые команды для отладки

```bash
# Просмотр всех логов
docker-compose logs

# Только последние 50 строк
docker-compose logs --tail=50

# В реальном времени
docker-compose logs -f

# Только ошибки
docker-compose logs | grep ERROR

# Проверка переменных окружения
docker-compose exec faq-telegram-bot env

# Интерактивный shell в контейнере
docker-compose exec faq-telegram-bot bash

# Статистика ресурсов
docker stats

# Проверка конфигурации
docker-compose config
```

---

## 📁 Структура проекта

```
telegram-hotel-faq-bot/
├── 📄 README.md                  # Быстрый старт
├── 📄 DOCS.md                    # Полная документация (этот файл)
├── 📄 PROJECT.md                 # Техническая документация (опционально)
├── 📄 LICENSE                    # MIT лицензия
├── 📄 CONTRIBUTING.md            # Для контрибьюторов
├── 🐍 bot.py                     # Основной код бота
├── 📋 requirements.txt           # Python зависимости
├── 🐳 Dockerfile                 # Docker образ
├── 🐳 docker-compose.yml         # Docker Compose конфигурация
├── 🔧 .env.example               # Пример переменных окружения
├── 🚫 .gitignore                 # Игнорируемые файлы
├── .github/
│   └── workflows/
│       ├── build-and-push.yml    # Build & Push workflow
│       └── deploy.yml            # Deploy on VPS workflow
└── screenshots/ (опционально)    # Скриншоты для документации
```

---

## ✅ Быстрая сводка

### Что необходимо знать разработчикам

1. **FAQ структура** в `bot.py` — просто editируйте словарь
2. **Docker Compose** управляет контейнерами
3. **GitHub Actions** автоматизирует деплой
4. **VPS** хранит .env с токеном (не в git!)
5. **Мониторинг** через `docker-compose logs -f`

### Стандартный цикл разработки

```bash
# 1. Отредактировать bot.py
nano bot.py

# 2. Проверить локально
docker-compose up -d --build
docker-compose logs -f

# 3. Запустить git-auto.sh
./git-auto.sh

# 4. GitHub Actions автоматически деплоит на VPS!
```

---

**Документация актуальна на:** `2025-11-21`

**Версия:** `1.0.0`

**Последнее обновление:** Комбинированная документация с архитектурой, CI/CD и развёртыванием

**Вопросы?** Откройте Issue на GitHub или прочитайте справку `/help` в боте.
